#include"iofile.h"

namespace Testor::IO{
    std::vector<std::string> ReadAllFile( const std::string& filepath ){
        std::vector<std::string> ret;
        ret.push_back( "OK" );
    return ret;
    }
}